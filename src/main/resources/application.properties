# Server Configuration
server.port=8080
server.servlet.context-path=/api
server.address=0.0.0.0

# Multipart upload limits
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=50MB

# Tomcat container limits (defaults are 2MB and will trigger 413 before Spring sees the request)
server.tomcat.max-swallow-size=50MB
server.tomcat.max-http-form-post-size=50MB

# Database Configuration

spring.datasource.url=jdbc:postgresql://ls-796e62adf0e6167084210449d55bfb367f63961a.cifyekjtqij4.ap-southeast-2.rds.amazonaws.com:5432/postgres?currentSchema=public
spring.datasource.username=postgres
spring.datasource.password=8w(Q;^yJ{6MSTs;89sb7[RAb<79`c{7]
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect



# JWT Configuration
jwt.secret=your_jwt_secret_key_should_be_long_and_secure
# 24 hours in milliseconds
jwt.expiration=86400000

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.com.pilgrimage=DEBUG

# Mail (SMTP) - values from SMTP_* or MAIL_* environment variables
spring.mail.host=${MAIL_HOST:${SMTP_HOST:smtp.gmail.com}}
spring.mail.port=${MAIL_PORT:${SMTP_PORT:587}}
spring.mail.username=${MAIL_USERNAME:${SMTP_USER:}}
spring.mail.password=${MAIL_PASSWORD:${SMTP_PASSWORD:}}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=${MAIL_FROM:${SMTP_FROM:${spring.mail.username}}}

# Frontend URL used in password-reset email links
app.frontend-url=${FRONTEND_URL:http://localhost:5173}

# CORS allowed origins (comma-separated)
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,https://53coxroadgallery.com,https://www.53coxroadgallery.com}

# Public contact form recipient
app.contact.email=${CONTACT_EMAIL:enquiries@53coxroad.com.au}

# Stripe (Test) - secret from environment variable STRIPE_SECRET_KEY
stripe.secret-key=${STRIPE_SECRET_KEY:}

# Pollinations image generation - key from environment variable POLLINATIONS_API_KEY
pollinations.api-key=${POLLINATIONS_API_KEY:}
